<section class="final-cta">
  <div class="container">
    <div class="final-cta-content">
      <h2 class="final-cta-title">
        <span class="cta-word">¿Listo para que tu negocio</span>
        <span class="cta-word italic yellow">escale?</span>
      </h2>
      <p class="final-cta-sub">
        Agenda una consulta gratuita y descubre cómo la tecnología puede
        multiplicar tus resultados. Sin compromiso, sin costo, sin presión.
      </p>
      <div class="final-cta-actions">
        <a href="#contacto" class="btn-primary btn-large" data-cursor-expand>
          Cotizar proyecto gratis
        </a>
        <a
          href="https://wa.me/51994660039"
          class="btn-whatsapp"
          target="_blank"
          rel="noopener noreferrer"
          data-cursor-expand
        >
          <span>💬</span> Escríbenos por WhatsApp
        </a>
      </div>
      <div class="final-cta-guarantee">
        <span>✓ Primera reunión gratuita</span>
        <span class="sep">·</span>
        <span>✓ Propuesta en 24h</span>
        <span class="sep">·</span>
        <span>✓ Máximo 8 proyectos por mes</span>
      </div>
    </div>
  </div>
</section>

<style>
  .final-cta {
    padding: 8rem 0;
    border-top: 1px solid var(--border);
    text-align: center;
  }

  .final-cta-content {
    max-width: 700px;
    margin: 0 auto;
  }

  .final-cta-title {
    font-size: clamp(2rem, 4.5vw, 3.5rem);
    line-height: 1.15;
    display: flex;
    flex-direction: column;
    gap: 0.1em;
    margin-bottom: 1.5rem;
  }

  .italic {
    font-style: italic;
  }
  .yellow {
    color: var(--yellow);
  }

  .final-cta-sub {
    font-size: 1.1rem;
    color: var(--white-muted);
    line-height: 1.7;
    margin-bottom: 2.5rem;
  }

  .final-cta-actions {
    display: flex;
    gap: 1rem;
    justify-content: center;
    flex-wrap: wrap;
    margin-bottom: 2rem;
  }

  .btn-large {
    padding: 1.1rem 2.5rem;
    font-size: 0.9rem;
  }

  .btn-whatsapp {
    padding: 0.9rem 2.25rem;
    border: 1px solid var(--border);
    color: var(--white-muted);
    font-size: 0.85rem;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    border-radius: 100px;
    transition: all 0.3s ease;
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    text-align: center;
  }

  .btn-whatsapp:hover {
    border-color: #25d366;
    color: #25d366;
    background: rgba(37, 211, 102, 0.1);
  }

  .final-cta-guarantee {
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 0.5rem;
    flex-wrap: wrap;
    font-size: 0.78rem;
    color: var(--white-muted);
    letter-spacing: 0.04em;
  }

  .sep {
    color: var(--yellow-dim);
  }

  @media (max-width: 768px) {
    .final-cta-actions {
      flex-direction: column;
      align-items: center;
    }
    .final-cta-guarantee {
      flex-direction: column;
      gap: 0.25rem;
    }
    .sep {
      display: none;
    }
  }
</style>


