<section class="trust-bar">
  <div class="container">
    <div class="trust-bar-inner">
      <span class="trust-item">✓ Primera reunión gratuita</span>
      <span class="trust-sep">·</span>
      <span class="trust-item">✓ Propuesta en 24h</span>
      <span class="trust-sep">·</span>
      <span class="trust-item">✓ Máximo 8 proyectos al mes</span>
    </div>
  </div>
</section>

<style>
  .trust-bar {
    border-top: 1px solid var(--border);
    border-bottom: 1px solid var(--border);
    padding: 1rem 0;
    background: var(--dark-2);
  }

  .trust-bar-inner {
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 0.75rem;
    flex-wrap: wrap;
  }

  .trust-item {
    font-size: 0.8rem;
    color: var(--white-muted);
    letter-spacing: 0.03em;
  }

  .trust-sep {
    color: var(--yellow-dim);
    font-size: 0.8rem;
  }

  @media (max-width: 600px) {
    .trust-bar-inner {
      flex-direction: column;
      gap: 0.35rem;
    }
    .trust-sep {
      display: none;
    }
  }
</style>
