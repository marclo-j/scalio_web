<section class="cta-section" id="cta">
    <div class="cta-inner">
        <h2 class="cta-title">¿Listo para escalar tu negocio?</h2>
        <a
            href="https://wa.me/51994660039"
            target="_blank"
            rel="noopener noreferrer"
            class="cta-button"
        >
            Charlemos por WhatsApp
        </a>
    </div>
</section>

<style>
    .cta-section {
        padding: 75px 0;
        position: relative;
    }

    .cta-inner {
        max-width: 1200px;
        margin: 0 auto;
        padding: 0 24px;
        text-align: center;
    }

    .cta-title {
        font-size: clamp(48.6px, 7.65vw, 97.2px);
        font-weight: 900;
        color: #f9cc15;
        line-height: 1.1;
        letter-spacing: -0.04em;
        margin-bottom: 40px;
    }

    .cta-button {
        display: inline-block;
        background: #f9cc15;
        color: #000000;
        font-size: 18px;
        font-weight: 700;
        padding: 16px 48px;
        border-radius: 8px;
        text-decoration: none;
        transition:
            transform 0.2s ease,
            box-shadow 0.2s ease;
    }

    .cta-button:hover {
        transform: scale(1.05);
        box-shadow: 0 8px 32px rgba(249, 204, 21, 0.35);
    }

    @media (max-width: 600px) {
        .cta-section {
            padding: 75px 0;
        }
    }
</style>
